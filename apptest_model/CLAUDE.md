from apptest_model import model
result_image = model(image)

이렇게 PIL 형태의 image를 넣으면 결과값을 출력하는 ai 모델을 만들것입니다.
이 때 이 모델은 어플리케이션에 태워서 어플리케이션이 정상 작동하는지 확인하기 위한 모델이기 때문에
실제로 학습할 필요는 없습니다. 오로지 어플리케이션이 정상적으로 작동하는지 확인하기 위해 그사이에 껴두는 모델입니다.

입력이미지를 PIL 형태로 받고 출력이미지도 PIL 형태로 내놓습니다. 이 때 입력이미지와 출력이미지는 임의의 이미지 크기를 받을 수 있도록 합니다.
즉, fc층은 없어야 합니다. 최대한 간단한 형태로 만들되 neral network 는 존재해야 합니다. conv 레이어트를 통과시켜주세요.

pytorch nn.module 기반으로 만들어주햣